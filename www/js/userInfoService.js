var OFFLINE_ONLY = true;

angular.module('starter').service('userInfoService', function ($http) {
    var userInfo = function(request) {
        if (OFFLINE_ONLY) {

        } else {
            
        }
    }
});